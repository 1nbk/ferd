// Intentionally empty — each route manages its own loading state.
// A root-level loading.tsx blocks every navigation with a full-screen spinner.
export default function Loading() {
  return null;
}
